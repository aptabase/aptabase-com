---
import StarOnGitHub from "./StarOnGitHub.astro";
import Logo from "./Logo.astro";
import SignUpOrDashboard from "./SignUpOrDashboard.astro";
import IconRss from "@tabler/icons/rss.svg?raw";
import IconCoin from "@tabler/icons/coin.svg?raw";
import IconSwitch from "@tabler/icons/switch-3.svg?raw";
---

<header x-data="{ isOpen: false }">
  <nav
    class="mx-auto flex max-w-7xl items-center justify-between p-6 md:px-8"
    aria-label="Global"
  >
    <div class="flex items-center gap-x-12">
      <a href="/" class="-m-1.5 p-1.5 flex items-center">
        <Logo />
        <span class="sr-only">Aptabase</span>
      </a>
      <div class="hidden md:flex md:gap-x-6">
        <a
          href="/blog"
          class="flex items-center space-x-1 rounded-lg py-1 px-2 text-gray-700 hover:bg-gray-100 hover:text-black"
        >
          <Fragment set:html={IconRss.replace('width="24"', 'width="20"')} />
          <span>Blog</span>
        </a>
        <a
          href="/#pricing"
          class="flex items-center space-x-1 rounded-lg py-1 px-2 text-gray-700 hover:bg-gray-100 hover:text-black"
        >
          <Fragment set:html={IconCoin.replace('width="24"', 'width="20"')} />
          <span>Pricing</span>
        </a>
        <a
          href="/integrations"
          class="flex items-center space-x-1 rounded-lg py-1 px-2 text-gray-700 hover:bg-gray-100 hover:text-black"
        >
          <Fragment set:html={IconSwitch.replace('width="24"', 'width="20"')} />
          <span>Integrations</span>
        </a>
      </div>
    </div>
    <div class="flex md:hidden">
      <button
        type="button"
        @click="isOpen = !isOpen"
        class="-m-2.5 inline-flex items-center justify-center rounded-md p-2.5 text-gray-700"
      >
        <span class="sr-only">Open main menu</span>
        <svg
          class="h-6 w-6"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          aria-hidden="true"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"
          >
          </path>
        </svg>
      </button>
    </div>
    <div class="hidden md:flex items-center gap-x-4">
      <StarOnGitHub />
      <SignUpOrDashboard />
    </div>
  </nav>

  <!-- Mobile menu, show/hide based on menu open state. -->
  <div class="md:hidden" role="dialog" aria-modal="true" x-show="isOpen">
    <div
      class="fixed inset-y-0 right-0 z-10 w-full overflow-y-auto bg-white px-6 py-6"
    >
      <div class="flex items-center justify-between">
        <a href="/" class="-m-1.5 p-1.5 flex items-center">
          <Logo />
          <span class="sr-only">Aptabase</span>
        </a>
        <button
          type="button"
          class="-m-2.5 rounded-md p-2.5 text-gray-700"
          @click="isOpen = !isOpen"
        >
          <span class="sr-only">Close menu</span>
          <svg
            class="h-6 w-6"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M6 18L18 6M6 6l12 12"
            >
            </path>
          </svg>
        </button>
      </div>
      <div class="mt-6 flow-root">
        <div class="-my-6 divide-y divide-gray-500/10">
          <div class="space-y-2 py-6">
            <a
              href="/blog"
              @click="isOpen = false"
              class="flex items-center space-x-2 -mx-3 text-lg rounded-lg px-3 py-2 leading-7 text-gray-900 hover:bg-gray-50"
            >
              <Fragment set:html={IconRss} />
              <span>Blog</span>
            </a>
            <a
              href="/#pricing"
              @click="isOpen = false"
              class="flex items-center space-x-2 -mx-3 text-lg rounded-lg px-3 py-2 leading-7 text-gray-900 hover:bg-gray-50"
            >
              <Fragment set:html={IconCoin} />
              <span>Pricing</span>
            </a>
            <a
              href="/integrations"
              @click="isOpen = false"
              class="flex items-center space-x-2 -mx-3 text-lg rounded-lg px-3 py-2 leading-7 text-gray-900 hover:bg-gray-50"
            >
              <Fragment set:html={IconSwitch} />
              <span>Integrations</span>
            </a>
          </div>
          <div class="py-6">
            <SignUpOrDashboard />
          </div>
        </div>
      </div>
    </div>
  </div>
</header>
